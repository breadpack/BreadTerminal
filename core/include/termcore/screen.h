#ifndef TERMCORE_SCREEN_H
#define TERMCORE_SCREEN_H

namespace termcore {

/// Terminal screen model: cell grid, cursor, scrollback (stub).
class Screen {
public:
    Screen() = default;
    ~Screen() = default;
};

} // namespace termcore

#endif // TERMCORE_SCREEN_H
